(trigger
    (key [[ctrl][shift]X]))

(name [List Files in Active Target])

(input nothing)

(output tooltip)

(save nothing)

(script [PROJECT_FILE=$(ruby -- "${TM_BUNDLE_SUPPORT}/bin/find_xcode_project.rb")
#PROJECT_FILE=${PROJECT_FILE##*/}

"${TM_BUNDLE_SUPPORT}/bin/xcode_project.rb" project=${PROJECT_FILE} list])